# Skip downloading the Electron binary during install in CI / headless
# environments where the GUI cannot run. Unset this locally on macOS to run
# the app. See GAPS.md.
ELECTRON_SKIP_BINARY_DOWNLOAD=1
